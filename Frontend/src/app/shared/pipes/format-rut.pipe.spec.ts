import { FormatRutPipe } from './format-rut.pipe';

describe('FormatRutPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRutPipe();
    expect(pipe).toBeTruthy();
  });
});
